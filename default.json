{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "DVWDesign canonical Renovate preset for PUBLIC repos \u2014 mirrors RootDispatcher/config/renovate-base.json (the private-repo source of truth). Consume via: \"extends\": [\"github>DESIGN-DVW/renovate-config\"]. Kept in sync by hand for now \u2014 see DISPATCH-094.",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    "helpers:pinGitHubActionDigests"
  ],
  "timezone": "Europe/Paris",
  "schedule": [
    "before 9am on Monday"
  ],
  "labels": [
    "dependencies"
  ],
  "prConcurrentLimit": 10,
  "prHourlyLimit": 4,
  "packageRules": [
    {
      "description": "Internal @design-dvw/* are private GitHub Packages governed by the shared-packages release + consumer-registry flow (shared-packages-first) \u2014 NOT by Renovate. Disabling avoids private-registry 'no-result' lookup failures (the Renovate bot has no read:packages auth). Security of these packages is managed at the shared-packages source; see DEPENDENCY-GOVERNANCE.md \u203a Internal packages.",
      "matchPackageNames": [
        "@design-dvw/{/,}**"
      ],
      "enabled": false
    },
    {
      "description": "Group all Storybook packages so they move together in ONE PR (they must stay on the same version). Storybook version alignment follows shared-packages (Main) \u2014 see docs/ecosystem/DEPENDENCY-GOVERNANCE.md.",
      "matchPackageNames": [
        "storybook",
        "@storybook/{/,}**",
        "@chromatic-com/storybook"
      ],
      "groupName": "storybook",
      "groupSlug": "storybook"
    },
    {
      "description": "NO AUTOMERGE, ANY update type. Policy: every dependency change lands through a reviewed PR \u2014 there is no unattended merge lane in this ecosystem (operator decision 2026-07-26; see DEPENDENCY-GOVERNANCE.md \u203a Merge policy and DECISION-RIGHTS.md). Staleness is handled by the PR-age watchdog that warns the operator, NOT by automerging. This rule is deliberately last so it overrides any automerge inherited from config:recommended.",
      "matchUpdateTypes": [
        "major",
        "minor",
        "patch",
        "pin",
        "digest",
        "lockFileMaintenance",
        "rollback",
        "bump",
        "replacement"
      ],
      "automerge": false
    },
    {
      "description": "Major version bumps are additionally labelled so the watchdog and reviewers can triage them first (human-gated per DECISION-RIGHTS.md)",
      "matchUpdateTypes": [
        "major"
      ],
      "labels": [
        "dependencies",
        "major-upgrade"
      ]
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": false,
    "description": "Renovate does NOT open security PRs. Dependabot owns security updates (org-enforced via DESIGN-DVW-org-config-1); this avoids duplicate security PRs. Renovate handles routine version updates only. See docs/ecosystem/DEPENDENCY-GOVERNANCE.md."
  }
}
